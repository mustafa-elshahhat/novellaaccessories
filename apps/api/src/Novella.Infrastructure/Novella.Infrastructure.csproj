<Project Sdk="Microsoft.NET.Sdk">

  <ItemGroup>
    <ProjectReference Include="..\Novella.Application\Novella.Application.csproj" />
    <ProjectReference Include="..\Novella.Domain\Novella.Domain.csproj" />
  </ItemGroup>

  <!-- Development seed images (original brand assets). Copied next to the app so the seeder can read
       them at runtime via AppContext.BaseDirectory. The .mjs generator is excluded (build tool only). -->
  <ItemGroup>
    <Content Include="SeedAssets\**\*.webp">
      <CopyToOutputDirectory>PreserveNewest</CopyToOutputDirectory>
      <Link>SeedAssets\%(RecursiveDir)%(Filename)%(Extension)</Link>
    </Content>
  </ItemGroup>

  <ItemGroup>
    <PackageReference Include="BCrypt.Net-Next" Version="4.2.0" />
    <PackageReference Include="CloudinaryDotNet" Version="1.29.2" />
    <PackageReference Include="Microsoft.EntityFrameworkCore.Design" Version="10.0.0">
      <IncludeAssets>runtime; build; native; contentfiles; analyzers; buildtransitive</IncludeAssets>
      <PrivateAssets>all</PrivateAssets>
    </PackageReference>
    <PackageReference Include="Microsoft.EntityFrameworkCore.SqlServer" Version="10.0.0" />
    <PackageReference Include="Microsoft.Extensions.Hosting.Abstractions" Version="10.0.0" />
    <PackageReference Include="Microsoft.Extensions.Http" Version="10.0.0" />
    <PackageReference Include="Microsoft.Extensions.Options.ConfigurationExtensions" Version="10.0.0" />
    <PackageReference Include="System.IdentityModel.Tokens.Jwt" Version="8.16.0" />
    <!--
      Pinned to override the vulnerable 10.0.0 pulled in transitively by the EF Core design-time
      tooling chain (EFCore.Design -> Microsoft.Build.Tasks.Core). 10.0.6+ fixes CVE-2026-33116
      (GHSA-37gx-xxp4-5rgx / GHSA-w3x6-4m5h-cxqf); 10.0.9 is the current servicing release.
    -->
    <PackageReference Include="System.Security.Cryptography.Xml" Version="10.0.9" />
  </ItemGroup>

  <PropertyGroup>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
  </PropertyGroup>

</Project>
